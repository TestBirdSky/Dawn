<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.sky6dbird.dawn">

    <application
        android:name=".App6D"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Dawn">
        <activity
            android:name=".m.MainActivity"
            android:launchMode="singleTask" />
        <activity
            android:name=".activity6d.Splash6DActivity"
            android:theme="@style/Theme.Splash6D"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".activity6d.Ser6DActivity" />
        <activity android:name=".activity6d.PrivacyActivity6D" />
        <activity android:name=".activity6d.Result6DActivity" />
    </application>

</manifest>